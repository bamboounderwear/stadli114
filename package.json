{
  "name": "baseball-workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "format": "prettier -w .",
    "migrate:generate": "drizzle-kit generate --config=drizzle/config.ts",
    "migrate:push": "echo \"Use \\\"wrangler d1 migrations apply\\\" after creating your D1 DB\""
  },
  "dependencies": {
    "hono": "^4.10.4",
    "drizzle-orm": "^0.33.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "drizzle-kit": "^0.23.2",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "wrangler": "^3.114.15"
  }
}